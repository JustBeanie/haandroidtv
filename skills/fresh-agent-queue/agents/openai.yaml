interface:
  display_name: "Fresh Agent Queue"
  short_description: "Queue every task to fresh, isolated agents"
  default_prompt: "Use $fresh-agent-queue to complete this task through fresh subagents with independent validation."
policy:
  allow_implicit_invocation: true
